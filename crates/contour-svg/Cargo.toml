[package]
name = "contour-svg"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
contour-model = { path = "../contour-model" }
proc-macro2 = { version = "1", features = ["span-locations"] }
syn = { version = "3", features = ["full"] }

[[bin]]
name = "cargo-contour"
path = "src/main.rs"

[lints]
workspace = true
