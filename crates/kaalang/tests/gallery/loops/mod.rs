//! The shapes one cycle can take. kaalang has no `while`, no `do`, no `for`
//! and no `continue`: every shape is a cycle whose question sits somewhere
//! else in the body, and iteration is a cursor wire like any other.

mod do_while;
mod endless;
mod for_each;
mod middle_exit;
mod while_loop;
